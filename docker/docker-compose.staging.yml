name: your-persona-ui-staging

networks:
  shared:
    external: true
    name: your-persona-shared

services:
  ui-staging:
    image: ghcr.io/aprxty3/your_persona_ui:${UI_IMAGE_TAG:-latest}
    restart: unless-stopped
    ports:
      - "127.0.0.1:3001:3000"
    networks:
      - shared
