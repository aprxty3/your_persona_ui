# Dependencies
node_modules/
# Next.js
.next/
out/

# Build
dist/
build/

# Secrets — NEVER commit (see AGENTS.md Security Rules)
# Every .env variant is ignored (including .env.production/.env.prod/.env.staging,
# which the old pattern did NOT cover) — only .env.example belongs in the repo.
.env
.env.*
!.env.example
# Credentials & certificates
*.pem
*.key
*.p12
*.pfx
secrets/
credentials/
# Cloudflare/deploy tokens that sometimes end up as local files
.dev.vars
wrangler.toml.bak

# IDE
.vscode/
.idea/
*.swp

# OS
.DS_Store
Thumbs.db

# Coverage & Testing
coverage/
*.lcov

# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*


# Vercel
.vercel

# TypeScript
*.tsbuildinfo
next-env.d.ts

# Documentation — internal/sensitive, generated outside source control
AGENTS.md
CHECKLIST.md
CLAUDE.md
TECHNICAL_DOCUMENTATION.md

# AI agent skills — internal workflow docs, machine-specific (same class as AGENTS.md)
.claude/
